name: pluggable
description: "A new Flutter package project."
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  event_bus: ^2.0.1
  use_case: ^1.1.6
  synchronized: ^3.1.0+1
  expire_cache: ^2.0.1

  pluggable_di_getit:
    git:
      url: git@github.com:camrongiuliani/pluggable.git
      path: packages/di/pluggable_di_getit
      ref: develop

  in_memory_storage_plug:
    git:
      url: git@github.com:camrongiuliani/pluggable.git
      path: packages/storage/in_memory_storage_plug
      ref: develop

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter: