name: pluggable_flutter
description: "A new Flutter project."
#resolution: workspace

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  pluggable:
    git:
      url: git@github.com:camrongiuliani/pluggable.git
      path: packages/pluggable
      ref: develop

  pluggable_di_getit:
    git:
      url: git@github.com:camrongiuliani/pluggable.git
      path: packages/di/pluggable_di_getit
      ref: develop

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

