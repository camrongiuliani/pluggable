name: pluggable
description: "A new Flutter package project."
version: 0.0.1
homepage: https://www.church.io
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  event_bus: ^2.0.1
  use_case: ^1.1.6

  pluggable_di_getit:
    path: ../di/pluggable_di_getit

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter: