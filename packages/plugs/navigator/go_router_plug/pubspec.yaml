name: go_router_plug
description: "A new Flutter package project."
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  go_router: ^14.2.0

  pluggable_flutter:
    git:
      url: git@github.com:camrongiuliani/pluggable.git
      path: packages/pluggable_flutter
      ref: develop

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
