name: dart_frog_server_plug
description: "A new dart project."
resolution: workspace

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  uuid: any
  dart_frog: ^1.0.0
  dart_frog_gen: ^2.0.0

  pluggable_dart_server:
    git:
      url: git@github.com:camrongiuliani/pluggable.git
      path: packages/pluggable_dart_server
      ref: develop

dev_dependencies:
  flutter_lints: ^5.0.0
